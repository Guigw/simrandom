type RootMutation {
    save(name: ChallengeToSave!, results: [ResponseToSave!]!): SaveChallengeDrawResponse!
}

type ChallengeDrawFormError {
    error: String
}

union SaveChallengeDrawResponse = SavedChallenge | ChallengeDrawFormError
